pipeline {
    agent any
    tools {nodejs "nodejs"}
    stages {
     /*stage('Install Node Modules') {
            steps {
                sh 'npm install'
            }
        }
         stage('Build') {
            steps {
                sh 'npm run build'               
            }
        }*/
        stage('Deploy to Ansible'){
            steps{
                sh 'zip -r build.zip build/'
               //withCredentials([usernamePassword(credentialsId:'Ansible_cred',usernameVariable:'username',passwordVariable:'password')]){
                    sh 'scp -i ~/home/jenkins/.ssh/id_rsa.pub /var/lib/jenkins/workspace/react/build.zip ansadmin@172.31.47.165:/home/ansadmin'
                //}
            }
        }

    }
}
